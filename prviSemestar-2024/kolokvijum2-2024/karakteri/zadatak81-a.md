```c
// Задатак 81:
// Саставити програм за:
// а)  унос текста са тастатуре све док се не унесе тачка (.) и приказује број карактера до тачке.
// б)  унос текста са тастатуре све док се не учита EOF и приказује укупан број карактера.

// ---------------------------------------------------------------------------------------------------------------

// а)  унос текста са тастатуре све док се не унесе тачка (.) и приказује број карактера до тачке.

#include <stdio.h>

int main()
{
    int c, n = 0;
    c = getchar();
    while (c != '.')
    {
        c = getchar();
        n++;
    }

    getchar();
    return 0;
}
```
