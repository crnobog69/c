def main {
    int x, y;
    
    // Using scan (simpler syntax):
    scan "Enter a number: " x;
    
    // Using read (requires = sign):
    read = "Enter another number: " y;
    
    print = "You entered:";
    print x;
    print y;
}

main;
judge 